import { PageContainer } from "../components/ui/PageContainer";

export function HomePage() {
  return (
    <PageContainer title="Bank-to-Ledger Reconciliation Platform">
      <p>Frontend shell is running.</p>
      <p>
        Future packs will add reconciliation queues, review workflows, and
        audit evidence views.
      </p>
    </PageContainer>
  );
}